const numero = Number(("50"))

if(numero > 0) 
  {console.log("Esse número passou no teste")}
	else { "Essa mensagem é secreta!!!"}
    {console.log( "Essa mensagem é secreta!!!")} //???????


// 1) a primeira linha está trocando o "50" string para "50" number

/* 2) qualquer número abaixo de 50 (que é a number que está no const) vai aparecer a mensagem:
"esse numero passou no teste" caso o numero seja maior que o const 50, a resposta será:
"essa mensagem é secreta!" e se o usuário colocar -10 no lugar do 0 não aparece nada no terminal.*/

// 3) sim.